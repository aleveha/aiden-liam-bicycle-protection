export const RESPONSIVE_IMAGE_WIDTHS = [360, 640, 960, 1280];

/** Full-bleed sections (hero, full-width banners): image is always viewport-wide. */
export const FULL_VIEWPORT_IMAGE_SIZES = "100vw";

/** Two-column layouts (e.g. card grids): image is half width at lg+. */
export const HALF_VIEWPORT_IMAGE_SIZES = "(min-width: 1024px) 50vw, 100vw";
